interface:
  display_name: "Review Changes"
  short_description: "Review local changes for serious defects"
  default_prompt: "Use $review-changes to review the current changes for correctness, security, and missing tests."
